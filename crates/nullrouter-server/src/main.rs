//! nullrouter — main server binary.
//!
//! Wires Actix-web (API + Leptos SSR via `leptos_actix`) on the public port
//! and supervises the Pingora-based MITM proxy on its own port.

fn main() -> anyhow::Result<()> {
    // Placeholder. Real entrypoint lands in Phase 7.
    Ok(())
}
