[package]
name = "nullrouter-caveman"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "NullStack Caveman — terse output compression (own brand, stacked with RTK)"
repository.workspace = true

[lib]
name = "nullrouter_caveman"
path = "src/lib.rs"

[dependencies]
serde.workspace = true
tracing.workspace = true

[lints]
workspace = true
