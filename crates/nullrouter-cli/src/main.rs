//! nullrouter-cli — setup and admin CLI.
//!
//! Ports `inspire/cli/` (install flow, start/stop, system tray, cert trust
//! helpers per-OS).

fn main() -> anyhow::Result<()> {
    // Placeholder. Real entrypoint lands in Phase 8.
    Ok(())
}
